# android

Kotlin + Jetpack Compose 네이티브 앱.

인증은 **public client + Authorization Code + PKCE** (AppAuth-Android). 토큰은 Keystore/EncryptedSharedPreferences에 보관하고, 백엔드 REST API는 Bearer JWT로 호출한다.

> 스캐폴드 예정 (Task #5).
